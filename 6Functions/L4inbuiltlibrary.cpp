#include<iostream>
#include<cmath>
using namespace std;
int sum(int x, int y){ //this arguemnet represents that while calling we are also sending some value
        return x+y;// return states the value of the function 
    }

int main(){
//    int x,y; 
//    cin>>x>>y;
//    cout << min(x,y);// it is built in function even if not declared it will give value
    cout<<sqrt(6.8);
}
// min(), max(),sqrt() are some built in functions